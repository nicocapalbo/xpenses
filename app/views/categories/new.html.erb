<%= form_with(model: @category, url: ledger_categories_path, local: true) do |f| %>
  <%= f.text_field :name, placeholder: 'Name of the category' %>
  
  <%= f.abyme_for(:subcategories) %>
  
  <%= f.submit 'Save' %>
<% end %>