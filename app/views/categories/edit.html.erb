<%= form_with(model: @category, url: ledger_category_path(@ledger, @category), method: 'PATCH', local: true) do |f| %>
  <%= f.text_field :name, placeholder: 'Name of the category' %>
  
  <%= f.abyme_for(:subcategories) %>
  
  <%= f.submit 'Save' %>
<% end %>